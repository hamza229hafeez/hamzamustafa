import { useEffect, useState } from 'react'; import { Menu, Moon, Sun, X } from 'lucide-react'; import { motion, useScroll } from 'framer-motion'; import { basePath } from '../config';
const links=['about','mobile-experience','projects','expertise','experience','contact'];
export default function Navbar(){const [theme,setTheme]=useState('dark'),[open,setOpen]=useState(false),[active,setActive]=useState('');const {scrollYProgress}=useScroll();
 useEffect(()=>{const saved=localStorage.getItem('theme')||'dark';setTheme(saved);document.documentElement.dataset.theme=saved;const obs=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&setActive(e.target.id)),{rootMargin:'-35% 0px -55%'});links.forEach(x=>{const n=document.getElementById(x);if(n)obs.observe(n)});return()=>obs.disconnect()},[]);
 const toggle=()=>{const t=theme==='dark'?'light':'dark';setTheme(t);localStorage.setItem('theme',t);document.documentElement.dataset.theme=t};
 return <><motion.div className="progress" style={{scaleX:scrollYProgress}}/><header className="nav"><a className="brand" href={basePath}>Hamza Mustafa<span>.</span></a><nav className={open?'open':''}>{links.map(l=><a key={l} className={active===l?'active':''} href={`${basePath}#${l}`} onClick={()=>setOpen(false)}>{l==='mobile-experience'?'mobile':l}</a>)}</nav><div className="nav-actions"><button className="icon-button" onClick={toggle} aria-label="Toggle color theme">{theme==='dark'?<Sun/>:<Moon/>}</button><button className="icon-button menu" onClick={()=>setOpen(!open)} aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></div></header></>
}
