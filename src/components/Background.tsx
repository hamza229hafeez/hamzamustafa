import { useEffect, useRef } from 'react';
export default function Background(){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current;if(!c)return;const x=c.getContext('2d')!;let w=0,h=0,raf=0,mx=-999,my=-999;const dots=Array.from({length:45},()=>({x:Math.random(),y:Math.random(),r:Math.random()*1.3+.4}));const resize=()=>{w=c.width=innerWidth*devicePixelRatio;h=c.height=innerHeight*devicePixelRatio};const move=(e:MouseEvent)=>{mx=e.clientX*devicePixelRatio;my=e.clientY*devicePixelRatio};const draw=()=>{x.clearRect(0,0,w,h);dots.forEach(d=>{const px=d.x*w,py=d.y*h,dist=Math.hypot(px-mx,py-my);x.beginPath();x.fillStyle=`rgba(130,110,255,${dist<180?0.45:0.16})`;x.arc(px+(dist<180?(px-mx)/dist*5:0),py+(dist<180?(py-my)/dist*5:0),d.r*devicePixelRatio,0,7);x.fill()});raf=requestAnimationFrame(draw)};resize();draw();addEventListener('resize',resize);addEventListener('mousemove',move);return()=>{cancelAnimationFrame(raf);removeEventListener('resize',resize);removeEventListener('mousemove',move)}},[]);return <canvas ref={ref} className="particles" aria-hidden="true"/>}
